// Generated by CoffeeScript 1.6.3
(function() {
  var basedirectory, directories, fromcol, hslLerp, tocol;

  basedirectory = "../Games/";

  directories = ["poopdogs", "megagame", "cool hero 8", "ohiforgotmyhat", "dontmesswithhexes", "afoolandhisbrunch", "EAT", "hihello"];

  hslLerp = function(from, to, t) {
    var col;
    col = {};
    col.h = from.h * (1 - t) + to.h * t;
    col.s = from.s * (1 - t) + to.s * t;
    col.l = from.l * (1 - t) + to.l * t;
    return col;
  };

  fromcol = {
    h: 314,
    s: 42.86,
    l: 44.8
  };

  tocol = {
    h: 202,
    s: 74.57,
    l: 64.61
  };

  $(function() {
    var col, dir, i, style, t, _i, _len;
    for (i = _i = 0, _len = directories.length; _i < _len; i = ++_i) {
      dir = directories[i];
      $.ajax({
        url: basedirectory + dir + '/data.json',
        async: false,
        dataType: "json",
        success: function(data) {
          return $("#mainContainer ol").append("<li id=\"slide" + i + "\">\n    <h2><span>" + data.title + "</span></h2>\n    <div class=\"slidecontent\">\n        <p>" + data.description + "</p>\n    </div>\n</li>");
        }
      }).fail(function(error) {
        console.log("Error parsing metadata for game '" + dir + "':");
        return console.log(error);
      });
    }
    style = (function() {
      var _j, _ref, _results;
      _results = [];
      for (i = _j = 0, _ref = directories.length; 0 <= _ref ? _j <= _ref : _j >= _ref; i = 0 <= _ref ? ++_j : --_j) {
        t = i / directories.length;
        col = hslLerp(fromcol, tocol, t);
        _results.push("#slide" + i + " h2 { background-color: hsl(" + col.h + ", " + col.s + "%, " + col.l + "%) }         #slide" + i + " div { background-color: hsl(" + col.h + ", " + col.s + "%, " + col.l + "%) }");
      }
      return _results;
    })();
    $("body").append("<style>" + style.join("\n") + "</style>");
    return $("#mainContainer").liteAccordion({
      "easing": "easeOutCubic",
      "containerWidth": $(window).width(),
      "containerHeight": $(window).height(),
      "headerWidth": 80,
      "slideSpeed": 400
    });
  });

}).call(this);

/*
//@ sourceMappingURL=buildpage.map
*/
